package com.example.crudcomJPA;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;


/* ATIVIDADE 4 ? - CRUD COM JPA
- O projeto deve efetuar a persistência da tabela no banco de dados.
- O projeto deve possuir as ações de CRUD seguindo a especificação da JPA e Injeção de dependência. */

@SpringBootApplication
public class CruDcomSpringApplication {

    public static void main(String[] args) {
        SpringApplication.run(CruDcomSpringApplication.class, args);
    }

}
